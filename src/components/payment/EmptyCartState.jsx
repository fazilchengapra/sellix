
export const EmptyCartState = ({ onNavigateHome }) => {
  return (
    
      No items in cart
      Add items to cart before proceeding to payment
      <button
        onClick={onNavigateHome}
        className="px-8 py-3 bg-blue-600 text-white rounded-xl font-medium hover);
};
