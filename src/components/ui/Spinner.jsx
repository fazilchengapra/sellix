
export const Spinner = ({ size = 'md', fullScreen = false }) => {
  const sizes = {
    sm: 'h-6 w-6',
    md: 'h-12 w-12',
    lg: 'h-16 w-16'
  };
  
  const spinner = (
    
  );
  
  if (fullScreen) {
    return (
      
        {spinner}
      
    );
  }
  
  return spinner;
};
